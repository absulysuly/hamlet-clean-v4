# Railway Monorepo Configuration
# This file helps Railway detect the monorepo structure

[build]
builder = "NIXPACKS"
root = "hamlet-clean/backend"

# Backend service should use hamlet-clean/backend as root
# Frontend service should use hamlet-clean/frontend as root
